# 上云部署方案（把定时任务从本机搬到服务器）

> 2026-09-21 制定。目标：**摆脱「电脑必须一直开着」**，让早报/晚报/选股/验证在服务器上自动跑。
>
> 状态：**代码已就绪，尚未在服务器上实测**（缺 LLM API key 与 ECS 实例）。

## 1. 为什么能搬 —— 三层架构里只有一层要改

原模式的瓶颈不在「脚本」，而在**中间的分析层**由桌面端 Agent 完成（读图、提炼、定性）。

| 层 | 组件 | 上云处置 |
|---|---|---|
| 抓取层 | `fetch_jy_article.js`（韭研）、`fetch_tgb.js`（淘股吧）、东财/新浪接口 | **原样可用** |
| 分析层 | 原为 WorkBuddy Agent | **已替换**为 `lib/llm.js` + `lib/prompts.js` |
| 写入与发布层 | `merge_report.js`、`check_codes.js`、`publish.sh`、`verify.js`、`screener.js`、`health_check.js` | **原样可用** |

## 2. 新增文件

| 文件 | 作用 |
|---|---|
| `tools/lib/llm.js` | LLM 调用层（OpenAI 兼容协议，零依赖）。支持文本与视觉、超时重试、JSON 解析容错、**按魔数判断图片类型** |
| `tools/lib/prompts.js` | 所有 prompt 集中定义（早报提炼 / 晚报结构化 / 读图转录） |
| `tools/job_morning.js` | 早报：取数 → LLM 提炼 → `merge_report` → `check_codes` → `publish` |
| `tools/job_evening.js` | 晚报：抓两个博主 → 切片 → 视觉模型转录 → LLM 结构化 → 写入 → 发布 |
| `tools/fetch_tgb.js` | 淘股吧取数器（列表 / 文章 / 正文图下载，含 `_max` 404 自动回退） |
| `tools/slice_image.py` | 长图切片（Pillow）。**必需** —— 实测有 530×6157 的超长表格图 |
| `tools/cron.sh` | cron 统一入口（时区断言 + flock 并发互斥 + 环境变量加载） |
| `tools/deploy_ecs.sh` | ECS 环境初始化（幂等） |

## 3. 环境变量（写在 `~/.astock.env`，**不入库**）

```bash
umask 077
cat > ~/.astock.env <<'ENV'
LLM_API_KEY=<你的 API Key，形如 id.secret>
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# 文本：免费档够用（每天只调 1~2 次，偶发 429 靠重试能扛过去）
LLM_TEXT_MODEL=glm-4.7-flash
# ⚠️ 必须关掉思考模式 —— glm-4.7-flash 是思考模型，不关则 content 恒为空
LLM_THINKING=disabled
# 视觉：**必须用付费档** —— 免费档 429 到不可用（见下方实测）
LLM_VISION_MODEL=glm-4.6v-flashx
# 可选
LLM_TIMEOUT_MS=180000
LLM_MAX_RETRY=5
LLM_VERBOSE=1
PYTHON_BIN=python3
ENV
```

- **必须用支持视觉的模型**（`LLM_VISION_MODEL`）：湖南人与行鱼的正文**全是图片**，没有文字捷径。
  仅凭这一条就排除了 DeepSeek（它没有开放的视觉 API）。
- **鉴权已自动适配两种格式**：智谱的 `<id>.<secret>` 会按 HS256 签 JWT，百炼 / DeepSeek 的
  `sk-xxx` 直接作 Bearer。换平台只改 `LLM_BASE_URL` 与模型名，**不用改代码**。
- 仓库根的 `.gh-token` 单独配置（需 `Contents: RW` + `Workflows: RW`）。

### 3.1 成本实测（2026-09-21，真实跑过量）

早报一次：输入 13.9K + 输出 2.5K token（文本）。晚报一次：9~60 张切片，每张约 2K 输入 / 600 输出。

| 环节 | 模型 | 实测成本 |
|---|---|---|
| 早报（文本） | `glm-4.7-flash`（免费档） | **¥0** |
| 晚报读图（视觉） | `glm-4.6v-flashx`（0.15 / 1.5 元每百万） | 9 张切片实测 **¥0.009** |
| 晚报汇总（文本） | `glm-4.7-flash`（免费档） | **¥0** |
| **合计** | | **约 ¥0.01/天 ≈ ¥0.3/月** |

**关于免费视觉档 `glm-4.6v-flash`**：实测**不可用** —— 连续 5 次 429（「该模型当前访问量过大」），
累计等待 135 秒才成功一次，而晚报要发 10~25 次视觉请求，根本跑不动。
付费档 `glm-4.6v-flashx` 不限流，全天不到 5 分钱。**这里不值得为省钱牺牲可运行性。**

## 4. 部署步骤

> 全部在**服务器上**执行。下面假设仓库放在 `/opt/astock`。

### 4.1 连上服务器

阿里云控制台 → 实例列表 → 点进实例 → 右上「远程连接」→ Workbench（网页终端，最省事）。
或本地终端 `ssh root@<公网IP>`（密码在控制台「重置实例密码」里设）。

### 4.2 装 git 并拉代码

```bash
dnf install -y git
cd /opt
git clone https://<你的PAT>@github.com/zoushiyun1999/astock-dashboard.git astock
cd astock
# 立刻把 token 从 remote URL 里摘掉，别让明文留在 .git/config
git remote set-url origin https://github.com/zoushiyun1999/astock-dashboard.git
```

### 4.3 环境初始化

```bash
sudo bash tools/deploy_ecs.sh
```

它会：设时区为 Asia/Shanghai → 装 Node 22 与 Python+Pillow → 配 git 身份 → 装 post-commit 钩子 → 自检。
**幂等**，任何一步失败都修好重跑即可。

**国内机房的两个下载源注意事项**（脚本已内置处理，出问题时按此排查）：

- **Node 22 不走 `rpm.nodesource.com`**。该源在阿里云内地 ECS 上经常只有几十 KB/s 甚至超时，
  卡住整条部署且报错信息指向不了真因。脚本按「就近」三级回退：
  ① `dnf module install nodejs:22`（阿里云内网镜像）→ ② `mirrors.aliyun.com/nodejs-release`
  的官方 tarball → ③ nodesource 保底。三条都不通才报错。
- **Pillow 走 RPM，不走 pip**。Alibaba Cloud Linux 3 的系统 `python3` 是 **3.6**（2021 年 EOL），
  Pillow 早已没有 cp36 预编译包 → pip 会退化成「源码编译」，而该 python 连 `_ctypes` 都没有、
  也缺 zlib 头文件，**必然失败**（2026-09-21 在真实 ECS 上实测）。
  正确做法是 `dnf install -y python3-pillow`（编译好的成品，几秒钟）。
  实在不行再装 `python3.11` 并用 `PYTHON_BIN=python3.11` 指定解释器。

> `tools/slice_image.py` 只用 `Image.open / crop / resize(Image.LANCZOS) / save`，
> 其中 **`Image.LANCZOS` 从 Pillow 2.7 到 12.3 都存在**（被删的是 `Image.ANTIALIAS`），
> 所以该脚本跨 Pillow 版本兼容，**不需要为新旧版本改代码**。

> **在 ECS 上手工敲命令时注意别名**：阿里云 Linux 沿用了 RHEL 对 root 的默认别名
> `alias cp='cp -i'`（`mv` / `rm` 同理）。所以**交互式** shell 里 `cp` 覆盖已存在文件会
> 停在 `cp: overwrite '…' ?` 等你确认，看起来像卡住。脚本内不受影响（非交互 shell
> 不展开 alias）；手工命令请用 `\cp`（反斜杠绕过别名）或直接加 `-f`。

### 4.3.1 服务器上更新代码（**不能用 `git pull`**）

内地机房 `git pull` 会报 `curl 52 Empty reply from server`（github.com 的 git 协议被干扰；
但同一台机器 `git clone` 可能成功，**时通时不通**）。而 **`api.github.com` 可用**
——发布链路本来就是走它。

**首选：`node tools/sync_from_api.js`**（默认只预演，加 `--apply` 才写）：

```bash
node tools/sync_from_api.js            # 列出所有与远端不一致的文件（不写盘）
node tools/sync_from_api.js --apply    # 实际同步
node tools/sync_from_api.js --only tools/lib/llm.js --apply   # 只同步指定文件
```

按 git blob 哈希逐文件比对，**写入前再复验一次哈希** → 不会落进半截内容。
同步范围与 `gh_push_api.js` 的推送范围完全一致。

🔴 **切换产线前必须跑一次**：`gh_push_api.js` 是**按文件内容差异**推送的——
本地若有**落后或多出**的旧文件，publish 时会把它**推回远端**、静默撤销别人的修复。
2026-09-22 实测：ECS 上的文件停在 09-21 20:05 的克隆状态，落后于远端的有
`tools/lib/llm.js`、`tools/job_evening.js`、`docs/上云部署方案.md`、`AGENTS.md`、
`dashboard/data.js` —— **一旦 publish 就会整体回退**。

**备用：手工按文件拉取**（工具不可用时）。让 API 直出原始字节
（`Accept: application/vnd.github.raw`），再用 `install` 落盘。
⚠️ **不要**改用 base64 + Python + `cp` 那套 —— 实测翻车两次：
- base64 解码后经 `print()` 输出会按 locale 重编码，中文可能被写成 GBK；
- `cp` 在阿里云 Linux 上被 alias 成 `cp -i`，交互式执行会停在 `overwrite?` 等确认。

```bash
cd /opt/astock
B=https://api.github.com/repos/zoushiyun1999/astock-dashboard/contents

curl -sL -H "Accept: application/vnd.github.raw" "$B/tools/publish.sh" -o /tmp/f1 && install -m 755 /tmp/f1 tools/publish.sh && echo "✔ publish.sh"
curl -sL -H "Accept: application/vnd.github.raw" "$B/tools/git-hooks/post-commit" -o /tmp/f2 && install -m 755 /tmp/f2 tools/git-hooks/post-commit && echo "✔ post-commit"
curl -sL -H "Accept: application/vnd.github.raw" "$B/tools/deploy_ecs.sh" -o /tmp/f3 && install -m 755 /tmp/f3 tools/deploy_ecs.sh && echo "✔ deploy_ecs.sh"
```

- `install -m 755` 无条件覆盖、不读别名、不受 `-i` 干扰（`mv`/`cp` 都会）。
- 仓库是公开的，**加不加 token 都行**（加了可避免匿名 60 次/小时的限流）。

**核验（必做，别用字节数判断）**：

```bash
git hash-object tools/publish.sh tools/git-hooks/post-commit tools/deploy_ecs.sh
git status --short
```

`git hash-object` 算的就是 git blob 哈希，与 GitHub API 返回的 `sha` 同一套算法——
相同才算文件真的对上。`git status --short` 应显示这三个文件为 ` M`（改了才会显示；
**什么都没显示就说明没覆盖成功**）。


### 4.4 配凭据

```bash
umask 077
cat > ~/.astock.env <<'ENV'
LLM_API_KEY=<智谱 API Key，形如 id.secret>
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
LLM_TEXT_MODEL=glm-4.7-flash
LLM_THINKING=disabled
LLM_VISION_MODEL=glm-4.6v-flashx
ENV

echo '<GitHub PAT>' > .gh-token     # 需 Contents:RW + Workflows:RW
chmod 600 .gh-token
```

### 4.5 验证（**先别装 cron**）

```bash
node tools/lib/llm.js --check                        # 配置自检
node tools/lib/llm.js --ping                         # 真实调用，验证鉴权
node tools/fetch_tgb.js --blog 444409 --list --n 3   # 机房 IP 能否抓淘股吧
node tools/fetch_jy_article.js --list --n 3          # 能否抓韭研公社
node tools/job_morning.js --dry                      # 早报全链路（不写盘）
node tools/job_evening.js --dry                      # 晚报全链路（会真的下载+读图）
```

**六条全过再往下走。** 尤其第 3 条 —— 它验证的是「机房 IP 会不会被财经站风控」，
这是整个方案里**唯一无法在本机预先验证**的未知数。

### 4.6 装 crontab

```bash
crontab -e
```

```
0  7  * * *    cd /opt/astock && bash tools/cron.sh health    >> logs/cron.log 2>&1
30 8  * * *    cd /opt/astock && bash tools/cron.sh morning   >> logs/cron.log 2>&1
10 15 * * 1-5  cd /opt/astock && bash tools/cron.sh screener  >> logs/cron.log 2>&1
0  21 * * *    cd /opt/astock && bash tools/cron.sh evening   >> logs/cron.log 2>&1
30 21 * * 1-5  cd /opt/astock && bash tools/cron.sh verify    >> logs/cron.log 2>&1
```

### 4.7 停掉本机的 7 个 automation

**⚠️ 顺序不能颠倒**：先让服务器正常跑**至少一天**、确认每日数据都对，**再**停本机任务。

两边同时写 `data.js` 会互相覆盖 —— `merge_report` 按日期幂等，**后写者静默覆盖前者**，
不会报错，只会出现「内容莫名其妙换了」。

## 5. 验收清单（按顺序，每步都要过）

```bash
node tools/lib/llm.js --check      # 配置自检；缺 key 退 1
node tools/lib/llm.js --ping       # 真实调用一次，验证鉴权与出网
node tools/fetch_tgb.js --blog 444409 --list --n 5    # 淘股吧可达性
node tools/job_morning.js --dry    # 早报：只抓取+提炼，不写盘
node tools/job_evening.js --dry    # 晚报：会真的下载图片并调用视觉模型
node tools/health_check.js         # 数据体检（有 WARN 退 1）
```

`--dry` 的输出要**人工比对**当天线上看板的内容，确认不是「格式对但内容空」。

## 6. 已知风险（未验证项）

| 风险 | 说明 | 应对 |
|---|---|---|
| **机房 IP 的风控差异** | 抓取源从家用宽带 IP 换成机房 IP，反爬策略可能不同 | 第 5 节的 `fetch_tgb.js --list` 就是探针；失真明显则改用家用常开设备方案 |
| **视觉模型读图质量** | 旧模式由 Agent 逐张读图；换成 API 后表格转录准确度**未实测** | 用 `--dry` 跑几天，对比人工核对 |
| **成本** | 晚报每天约 50~60 张切片送模型（湖南人 6 图、行鱼可达 49 图） | 随时可用 `--max-slices N` 封顶；观察一周后再定 |
| **ECS 试用到期** | 额度耗尽或满 3 个月 → **实例自动释放，数据只保留 72 小时** | 见第 7 节 |
| 投资日历未接入 | 原晚报含「投资日历专版」（韭研公社不定期发布） | **尚未实现**，见第 8 节 |

## 7. 试用期到期前必须做的事

`publish.sh` 已经把数据同步到 GitHub，所以**数据本身不怕丢**。但要无缝切换：

1. 在**释放前**购买正式实例（轻量应用服务器量级即可，脚本全部零依赖）。
2. 新机器上重跑 `deploy_ecs.sh` + 恢复 `~/.astock.env` 与 `.gh-token`。
3. 老实例释放前确认 `node tools/health_check.js` 与线上页面正常。

## 8. 尚未实现 / 后续

- **投资日历专版**：`job_evening.js` 目前不产出 `calendar` 字段（该字段可省略，`merge_report` 允许）。
  补齐需要：抓韭研公社日历博主页 → 下载原图 → 用视觉模型判断「是否真含日历内容」
  （历史坑：抓到过一张 2.2MB 的牡丹花装饰图，必须校验）→ 写入顶层 `calendar`。
- **两个看门狗 automation**（早报补跑 10:00 / 晚报补跑 22:30）：上云后**冗余，可直接停用** ——
  它们的存在意义就是「本机可能没开机」。服务器常驻后，失败会由 `logs/ALERT.md` 与
  `health_check.js` 兜住。

## 9. 回滚

本方案**不改动**任何现有脚本的行为，只新增文件。回滚 = 停掉服务器 crontab + 重新启用本机
7 个 automation。旧路径（Agent 驱动）完全保留可用。

## 10. 实测记录（2026-09-21，本机验证部分）

- 淘股吧 `m.tgb.cn` 从本机可达；**不带尾部斜杠**才是 200（`/blog/444409/` 会 404）。
- 博客列表日期是完整格式 `2026-09-21 17:04`；列表实测**降序**（与旧文档「正序」说法相反）→
  代码里按时间自行排序，不依赖页面顺序。
- 正文图：`loadImg(this,"…_max.png")` + `data-original="…_760w.png"`；`src` 恒为占位符。
  **`_max` 可能 404（CDN NoSuchKey）** → 必须回退 `_760w`。
- **URL 后缀 `.png` 但实际内容是 JPEG**（魔数 `ff d8 ff e0`）→ 一律按魔数判断。
- 两个博主的正文**都是图片，页面无正文文字**。旧 prompt 称「正文文字在 `gtgioMsg` 的
  subject 属性里」是**错的** —— `gtgioMsg*` 是评论区。
- 长图必须切片：530×6157 整张交给模型完全无法阅读；按 1200px/段 + 80px 重叠 + 1.5x 放大
  切成 6 段后可清晰读出「市场连板股」表格（代码/名称/末次时间/连板数/原因）。

## 11. ECS 验收记录（2026-09-21 实测）

实例：`i-bp1424q6g61zeiejozhv`，华东1（杭州），Alibaba Cloud Linux 3.2104 LTS，
`ecs.e-c1m1.large`（2核2G），公网 `101.37.237.83`。

### 11.1 已通过 ✅

| 项 | 结果 |
|---|---|
| 环境初始化 `deploy_ecs.sh` | 全绿（时区 +0800、Node v22.22.2、Pillow 5.1.1、git 身份、post-commit 钩子 + 自检） |
| 凭据 | `.gh-token` 94B，PAT 实测 HTTP 200；`~/.astock.env` 6 项，key 长 49，`--ping` 连通 |
| 数据源可达性 | **淘股吧 + 韭研公社均稳定可达**，机房 IP 未被风控 |
| 早报全链路 `--force --dry` | ✔ 抓取→提炼→`merge_report` 校验通过；status 三段式、H8 门槛过 |
| 晚报全链路 `--dry` | ✔ 65 张切片读完；`明日关注 4 / 板块热点 4` 条数一致；picks 代码正确 |

### 11.2 遗留问题

> **状态更新 2026-09-22 14:20**：P0 与两个 P1 已解决。下面**保留「已排除的假设」原文**
> —— 它们是本次最大的收获，下次遇到同类现象不要再重复试。

**✅ 已解决 · 东财 `push2delay` 间歇不可用**（原 P0，曾阻塞「量价选股」上云）

结论：**不是东财在封服务器，而是密集探测触发了反爬。**

| 时间 | 结果 |
|---|---|
| 09-21 21:04 | ✔ 3 次成功 |
| 09-21 21:05 | ✘ `screener.js` → `全市场 0 只`，`real 0m1.587s` |
| 09-21 21:12–21:24 | ✘ 连续 `000`（>12 分钟，静置 3 分钟无效） |
| **09-22 14:06** | **✔ 恢复**：`total:3489` + 真实 `diff` |
| **09-22 14:06** | **✔ `screener.js --dry` → 全市场 3489 只 → 入选 15 只，`real 0m2.065s`** |

对照：本机此前记「东财 `push2*` 全系被持续重置」——**同一套域名在 ECS 上正常**，
说明那条旧结论的归因也是错的（本机 `screener` 同样在密集翻页，反复触发同一机制）。

**已排除的假设**（都验证过，不要再重复试）：
1. ~~「服务器上东财完全正常」~~ —— 21:09 那 14/14 是**成功窗口**，不是常态。
2. ~~「Node fetch 与 curl 行为差异」~~ —— 失败窗口里**两者同时失败**，与客户端实现无关。
3. ~~「简化请求 vs 完整请求的参数差异」~~ —— 同一窗口内两者同为成功/同为失败。
4. ~~「短时限流、几分钟自动解封」~~ —— 静置 3 分钟无效，连续失败已 >12 分钟。

⚠️ **操作纪律（本次最大的方法论收获）**：
- **禁止用密集循环探测有反爬的站点**。09-21 我在 10 分钟里打了约 40 次请求，
  **把「观测」做成了「制造故障」**，然后在自己制造的故障里找规律，越找越乱。
- 正确姿势：**单次请求 → 等足够久 → 再看一次**；判据用**持续成功率**
  （如每 10s × 15 次），不用单次结果。
- 观测尺度必须匹配现象尺度：秒级现象用秒级采样，**小时级现象必须小时级采样**。

⚠️ **`screener.js` 的上游闸门必须保留**（交易日 + `market.length===0` → `exit 2`、不写盘）
—— 它是防止把「网络失败」写成「入选 0 只」假数据的唯一防线。

**⏳ 未解决 · 晚报读图有若干部位 `无法解析模型输出为 JSON`**（已跳过、未中断任务）
- 根因：单张图内容过多 → 模型输出超上限被截断。**与批次大小无关**
  （`BATCH=1` 单张图仍然失败）。
- 已完成的可观测性改动：① 错误信息补充**总长度 + 花括号配对数 + 首尾各 200 字**
  （不配对 ≈ 截断，配对 ≈ 格式问题）—— 此前只打前 300 字，看不出是否截断，
  导致把「裸换行」误判成「token 不足」而白调两轮参数；② 失败清单落盘
  `tmp_evening_failures_<date>.json`（此前只能从终端滚屏找，事后不可追溯）。
- 待做：解析失败时对**该图用更小切片粒度**（1200→800px）重读一次。
  **先拿到真实失败样本再定参数**，不要拍脑袋。

**✅ 已解决 · `BATCH=1` 是基于误判的改动，已调回 2**
当初从 2 降到 1 的理由是「输出被 token 截断」，而真因是 **JSON 字符串里的裸换行**（已修）。
单张图仍会失败，证明批次与截断无关。调回 2 后视觉调用次数减半。

**✅ 已解决 · prompt 空值/占位符漏洞 + 代码层兜底**（09-22 新增，源自 `merge/evening` 告警）

告警实证：模型在 `picks` 里输出了**字面的 `"..."`** 与**空字符串**，而 `merge_report`
只校验「非空」→ 占位符能过校验、然后原样进看板展示给读者。两层修复：

1. **prompt 根因**：`BASE_RULES` 原文写「没有就给空数组或空字符串」——**这条本身就是
   空值的来源**。已改为「数组字段给 `[]`，文字字段写『未提及』」，并新增独立条款
   禁止 `...` / `…` / `—` / `待补` / `N/A` 之类占位符。
   `EVENING_SHAPE` 的 `name`/`reason` 示例原本是空串（**模型会照抄示例**），改为非空示例。
   另补：`picks[].status` 含多个板数（如「4板+10天5板」）时**按最高板数**判定
   —— 这正是告警里被判定为 5 板的原因，原 prompt 完全没说明。
2. **代码兜底**：`job_evening.js` 新增 `clean()`，纯符号占位符一律清空，
   让它走「校验失败 → 失败原因反馈给模型重试一次」这条**已被实证有效**的路径
   （09-21 实盘：第 1 次校验失败、第 2 次通过）。**不替换成「未提及」之类的文案**
   —— 那是把「没读到」伪装成「已读到」。
   已用 38 例断言：真实样本 `"..."` → 清空；`4板+10天5板+低价` / `暂无` / `N/A 待确认`
   一字不改（正则**故意收窄**：误伤会丢真内容，代价远大于漏判）。

**✅ 已解决 · 服务器上的假告警**（原 P2）：3 条已在服务器闭环
（`20:54 merge/evening` 是真告警但已被自动重试解决；
`21:06` / `21:11 screener ABORT` 系密集探测东财所致，非数据源故障）。

闭环手法（**易踩坑**）：`ops.js --append-alert --result CLOSED` **不能闭环已有条目**
—— 它是追加一条新条目，`--list-open` 仍是 3。必须**原地改结果字段**，
本次用锚定时间戳的 `sed`（已在本地样本上断言：3 条目标行变 CLOSED、无关条目零误改）。
另注：`logs/` 在 `gh_push_api.js` 的 `SKIP_DIR` 内 → **这些告警不会被推到线上仓库**，
只影响服务器本地体检结果。

**P2 · 投资日历未接入**：`job_evening.js` 目前不产出 `calendar` 字段
（该字段可省略，`merge_report` 允许）。详见第 8 节。

### 11.3 切换计划与进度（本机 → 服务器）

🔴 **真正的风险不是「同时写」，而是「落后的一侧把旧文件推回」**

`gh_push_api.js` 按**文件内容差异**推送 —— 只推「本地与远端不同」的文件。所以两个环境
交替发布时，落后的一侧一发布就会把它手里的旧文件推回远端、**静默撤销另一侧的成果**。
具体会发生的两幕（2026-09-22 都验证过）：

| 场景 | 后果 |
|---|---|
| 服务器 15:10 写完 `dashboard/screener.js` → 本机 21:00 发布 | 本机把**陈旧 screener.js 推回**，当天选股被抹掉 |
| 服务器 21:30 写 `data.js`（verify 标记）→ 本机次日 08:30 早报发布 | 早报把 **verify 标记抹掉** |

**解法（2026-09-22 已实现）：写盘前预同步。**
- `tools/lib/pre_sync.js`：4 个写入型任务在**读/写 `data.js` 之前**先取回远端最新版
  （只同步 `dashboard/data.js` + `dashboard/screener.js`，不碰代码）。**fail-open**。
- `tools/cron.sh`：服务器侧在 flock 之后再做一次**全量**预同步（代码也不该落后）。

⇒ **有了它，就可以逐任务增量迁移，不需要「停机切换」**（这是本节的结论修正：
旧版写的"必须停机切换"过于保守，根因是当时没有预同步）。

**每搬一个任务仍有两条硬规矩**：
1. **搬之前先停本机对应 automation**（避免同一时段两边都跑）。
2. **时段要错开**：若今天已经过了该任务的时段、且本机今天还会跑，就别急着装 cron
   （否则今天会双跑一次）。`screener` 15:10 就是这种情况 → 15:10 之后再装。

**进度（2026-09-22）**

| 任务 | 时段 | 状态 |
|---|---|---|
| `health` | 每天 07:00 | ✅ 已在服务器 cron（不写盘，零风险） |
| `verify` | 工作日 21:30 | ✅ 已在服务器 cron；本机 automation 已 PAUSED。**2026-09-22 21:30 是首个无人值守写入型任务** |
| `screener` | 工作日 15:10 | ⏳ 待 15:10 之后装 cron + 停本机（当天先由本机产出） |
| `morning` | 每天 08:30 | ⏸ 暂缓——它的 LLM 质量还需观察几天 |
| `evening` | 每天 21:00 | ⏸ 同上（读图质量最需要观察） |

全部稳定运行 3~5 天后再删本机的 7 个 automation。

装 crontab 的命令见 §4.6（注意：**每次重装都用 `grep -v 'tools/cron.sh'` 后重写全部三行**，
否则会漏掉已有的行）；本机 automation 的停用见 §4.7。

### 11.4 试用期死线

🔴 **2026-12-21 23:59:59** 实例到期 → 自动释放，数据只保留 72 小时。
到期前必须完成：① 决定是否续费，或 ② 迁移到新实例（`deploy_ecs.sh` 幂等，重装成本约 10 分钟）
并恢复两个凭据文件。

