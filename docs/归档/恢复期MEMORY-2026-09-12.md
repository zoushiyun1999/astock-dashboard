# MEMORY.md

> 项目长期记忆。工作中学到新东西主动回写，不等提醒。
> 凭据只记位置不记值；代码里能查到的东西不往这里复制。

## 项目快照

- **项目名**：2026-09-12-18-38-12（待命名）
- **当前阶段**：空工作区初始化；本次会话定位历史「A股简报」网页记录
- **最后更新**：2026-09-12

## 架构决策记录（ADR）

| 日期 | 决策 | 原因 | 放弃的方案 |
| --- | --- | --- | --- |
|  |  |  |  |

## 环境事实

- 宿主：Windows（win32），Git Bash，用户 `zoush`，主机名 `DESKTOP-NQT2JG0`
- 兄弟工作区列表（`D:\workbuddylujing\` 下按时间戳命名，无语义）：
  `2026-09-10-22-28-02`、`2026-09-11-19-27-15` ~ `2026-09-11-20-31-59`、`2026-09-11-22-13-32`、`2026-09-12-18-38-12`

### 「A股简报」资产清单（2026-09-12 核实）

**本地产物（全部保留可用）** —— 位于 `D:\workbuddylujing\2026-09-11-20-31-59\local\`：

| 文件 | 说明 |
| --- | --- |
| `web/index.html` | 简报网页本体（23KB，标题 `stock-lens`）；用 `<script src="data.js">` 加载数据，双击即可打开 |
| `web/data.js` | 由 `update.py` 生成的数据（`window.DATA = {...}`） |
| `data.snapshot.json` | 数据快照，**单份覆盖式，无历史归档** |
| `update.py` / `update.bat` / `publish.bat` | 取数 / 取数+发布脚本 |
| `logs/` + `update.log` | 每轮运行日志（含历史，可追溯） |
| `config.json` | 数据源配置（4 个选股预设 + 4 个博主源） |

**线上记录（均已下线，实测 HTTP 404）**：

| 地址 | 状态 |
| --- | --- |
| `web-nn7cvqpg-vx7fkk24.edgeone.cool`（EdgeOne 匿名部署） | 404，匿名部署本身 1 小时过期 |
| `asx.79zl.cn` | 404，DNS 解析待删 |
| `zoushiyun1999.github.io/astock-dashboard` | 404，GitHub Pages 已关 |
| WorkBuddy 部署 `d888422f04f6486ea7f46814e6da6a7a` | 已 unpublish |

> 相关 GitHub 仓库 `zoushiyun1999/astock-dashboard` 仍存在且为 public，但全部 5 个 workflow 已 `disabled_manually`。

## 踩坑与结论

- **数据快照是覆盖式的**：`data.snapshot.json` 与 `web/data.js` 每次取数直接重写，**没有历史版本**。想要留存某天的简报，必须在当天复制一份，或改用带日期的文件名归档。
- **线上能下线干净，是因为查了"三层触发源"**：① WorkBuddy automation ② GitHub Actions workflow ③ Windows 计划任务。只删一层会继续跑。

## 用户偏好与纠正

- 拒绝谄媚：不要"这是个好问题"式开场，不要夸需求。
- 需要选择时给 A/B/C 方案 + 权衡，不要开放式提问。
- 偏好简洁清爽的 UI。
- 每个项目根目录维护 `AGENTS.md` + `MEMORY.md`（模板源 `~/.codex/templates/`）。
- 个人文件操作：先备份、隔离区优先、绝不直删。

## 历史资产位置（2026-09-10 大清理时移出，未抹除）

> 用户 2026-09-10 22:35 清空了所有 WorkBuddy 数据。**原始文件未删除，只是移到隔离区**，可完整恢复。

### 「A股速览 · 每日简报」原系统 —— `D:\WorkBuddy_删除隔离_2026-09-10\WorkBuddy\A股推送系统\`（35MB）

| 内容 | 路径 | 说明 |
| --- | --- | --- |
| **简报网页** | `dashboard/index.html` | 标题「A股速览 · 每日简报」，5 个 Tab：早报 / 晚报 / 短线 / 量价 / 日历 |
| 数据 | `dashboard/data.js`（171KB）、`data.json`（133KB） | `window.REPORTS`，停止时 `updatedAt=2026-09-10 14:30`，7 份 report |
| 选股数据 | `dashboard/screener.js` | 量价选股独立数据（与 data.js 隔离） |
| 渲染逻辑 | `dashboard/js/app.js`（29KB） | 全部 Tab 渲染 |
| 样式 | `dashboard/css/style.css` | 主题样式 |
| **4 个自动化定义** | `.workbuddy/automations/automation-*/memory.md` | 见下表 |
| **11 天运行日志** | `logs/2026-08-31.md` ~ `2026-09-10.md` | 逐日执行记录 |
| 文档 | `docs/`（14 个文件） | 系统总览、选股逻辑、云端方案、上线清单等 |
| git 历史 | `.git/`（102 次提交） | 另有 bundle 备份 |

**原定时任务清单（共 7 个，清理时 6 个已删、1 个云端残留）**：

| 任务名 | 时间 | 说明 |
| --- | --- | --- |
| A股早报-开盘必读资讯 | 每天 08:30 | 抓韭研公社开盘必读，提炼关注股 |
| A股早报补跑看门狗 | 每天 10:00 | 早报漏跑补执行 |
| A股量价选股 | 工作日 14:30 | 3486 只 → 基础 → 连阳 → 均线上方 |
| A股晚报-AI读图复盘 | 每天 21:00 | 湖南人读图 + 行鱼复盘 + 投资日历专版 |
| A股次日验证 | 工作日 21:30 | 验证前日「明日关注」命中率 |
| A股晚报补跑看门狗 | 每天 22:30 | 晚报漏跑补执行 |
| 生成昨日 AI 重点资讯总结 | 云端 | **未能删除**（接口只读） |

**原线上地址（全部 404）**：`asx.79zl.cn`（自有域名）、`zoushiyun1999.github.io/astock-dashboard`、WorkBuddy 沙箱 `d888422f04f6486ea7f46814e6da6a7a.app.workbuddy.link`（已 unpublish）。

**遗留安全项**：隔离区内 `.gh-token` 仍有效，需到 https://github.com/settings/tokens 吊销（只删文件不够）。

### 其它隔离内容

- `D:\WorkBuddy_删除隔离_2026-09-10\WorkBuddy\`：`小说设定`、`Claw`、6 个旧工作区
- `D:\WorkBuddy_删除隔离_2026-09-10\workbuddylujing\`：22 个旧工作区（2026-09-03 ~ 09-10）
- `D:\WorkBuddy_删除隔离_2026-09-10\dotworkbuddy-projects\`：全部历史会话 jsonl（其中「A股推送系统」80 个文件）
- `D:\WorkBuddy_删除隔离_2026-09-10\清理收尾清单.md`：完整的清理记录与手动待办
- `D:\WorkBuddy_备份_2026-09-10\`：177MB 完整备份 + `astock-dashboard-full.bundle`

**复活方式**：把隔离区内容移回原位即可恢复。清空这两个文件夹 = 彻底释放 317MB。

### 重建资料（2026-09-12 产出，存放在与会话无关的稳定目录）

**目录**：`D:\workbuddylujing\A股简报-重建资料\`

| 文件 | 内容 |
| --- | --- |
| `一键恢复提示词.md` | ⭐ 复制粘贴到新对话即可自动完成恢复的完整提示词 |
| `A股简报系统-重建文档.md` | 重建前置条件 / 6 个任务提示词 / 系统规格 / 硬性规则 / 21 条踩坑清单 / 已知缺陷 |
| `A股简报-定时任务提示词.md` | 6 个任务的纯 prompt，可直接粘贴到 automation 的 prompt 字段 |

**恢复目标目录约定**：`D:\workbuddylujing\A股推送系统\`

> ⚠️ **原定时任务 prompt 全文已丢失**（随任务删除）。上述提示词依据隔离区 4 份 `automation-*/memory.md` 重建，覆盖全部已知执行步骤与规则，但不是原文。
> 💡 恢复时凭据必须换新（`.gh-token` 与 Server酱 key 从未轮换）。
> 💡 量价选股调度建议从原 14:30 改为 15:10（盘中跑会把入选数从 51→18 压到 22→2）。

### ✅ 恢复已执行（2026-09-12 19:42，在**另一个会话**中完成）

用户在别处跑了 `一键恢复提示词.md`，项目已落地到 `D:\workbuddylujing\A股推送系统\`（33MB）：

| 项 | 状态 |
| --- | --- |
| 文件复制 | ✅ 完成，隔离区原件未动（仍可作为安全网） |
| git 历史 | ✅ **102 次提交完整**，working tree 有未提交改动 |
| 看板数据 | ✅ `dashboard/version.json` 的 `exportedAt=2026-09-12 19:42`，说明恢复后跑过一次 export |
| 本地预览 | ✅ `http://127.0.0.1:8899/` 现**由恢复副本提供服务**（非隔离区原件） |
| 6 个定时任务 | ❓ 项目内 `.workbuddy/automations/` 只有 4 份 memory.md 副本；**全局 automations 目录为空**，未见注册痕迹 |
| 凭据换新 | ❓ 未见记录 |

**新增待办**：确认恢复会话是否走完第 5 步（注册 6 个定时任务）与第 6 步（换新凭据）；核对 `git status` 中未提交的 `MEMORY.md` / `dashboard/index.html` / `version.json` 改动是否该提交。



## 外部资源位置

| 资源 | 位置 | 备注 |
| --- | --- | --- |
| 项目约束/记忆模板 | `~/.codex/templates/` | AGENTS.md、MEMORY.md |
| A股行情 CLI（westock Go） | `C:\Users\zoush\.local\bin\westock.exe` | `quote` / `hot sector` / `sector ranking` 等，无 JSON 开关 |
| 选股 CLI（westock-tool） | `~/.workbuddy/plugins/cache/cb_teams_marketplace/finance-data/1.5.0/skills/westock-tool/scripts/index.js` | Node ≥18，免 npm install |
| EdgeOne 部署令牌（匿名） | `2026-09-11-20-31-59\local\web\.edgeone\anonymous.json` | 已过期（2026-09-11 23:04） |

## 待办 / 悬置问题

- [ ] 本工作区（`2026-09-12-18-38-12`）的项目名与目标待用户确认 —— 目前为空目录
- [ ] 是否为「A股简报」增加**按日期归档**的机制（现在只有一份覆盖式快照）
- [ ] 是否恢复线上访问（若需要，EdgeOne 平台子域名实测国内最快，且免备案）
- [ ] 沿用历史遗留待办：删 GitHub 仓库 `zoushiyun1999/astock-dashboard`、吊销 `.gh-token`、删 `asx.79zl.cn` DNS
