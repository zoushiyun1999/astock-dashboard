# A股简报系统 · 一键恢复提示词

## 怎么用

1. 在 WorkBuddy 里**新开一个对话**
2. 把下面代码块里的**全部内容**复制粘贴进去，发送
3. 剩下的交给它 —— 它会先读资料、复述计划，等你确认后再动手

> 建议新对话的工作目录选 `D:\workbuddylujing\A股推送系统`（恢复目标），或任意目录，提示词里已写明绝对路径。

---

```text
恢复我的「A股推送系统」（网页看板标题叫「A股速览 · 每日简报」）。

【背景】
这套系统是我 2026-09-10 从 WorkBuddy 清空的，一共 6 个定时任务 + 1 个网页看板 + 一整套零依赖脚本。
清空时文件没有真删，完整保存在隔离区。我要把它恢复到能跑的状态，并重建全部定时任务。

【源文件位置（只读，不要改动）】
D:\WorkBuddy_删除隔离_2026-09-10\WorkBuddy\A股推送系统\
35MB，含：dashboard/（网页站点）、tools/（全部脚本）、config/、docs/、logs/（11天运行日志）、.git/（102次提交）

【参考文档（第一步就读，不要跳过）】
- D:\workbuddylujing\A股简报-重建资料\A股简报系统-重建文档.md
- D:\workbuddylujing\A股简报-重建资料\A股简报-定时任务提示词.md
这两份文档含：完整系统规格、数据结构、5个Tab的渲染逻辑、6个定时任务的提示词全文、
11条硬性规则、21条踩坑清单、原系统的11个已知缺陷。
如果读不到这两份文件，停下来告诉我，不要凭猜测动手。

【执行步骤】

第一步：读完两份文档后，用一段话向我复述你理解的恢复范围，然后停下等我确认。不要跳过我直接开工。

第二步：把项目复制到 D:\workbuddylujing\A股推送系统\（用复制，不要移动，隔离区原件必须保留）。

第三步：验证脚本能跑 ——
  node tools/health_check.js      （看板数据体检）
  bash tools/bump_version.sh      （刷新版本号 + 排序 + 导出 JSON）
  然后在 dashboard/ 下起本地预览：
  python -m http.server 8899 --bind 127.0.0.1
  打开 http://127.0.0.1:8899/ 确认能看到 5 个 Tab 且有历史数据。

第四步：重建 .git/hooks/post-commit（源文件在 tools/git-hooks/post-commit）。
  这是本地提交自动上云的唯一通道，缺了它数据会停在本地、线上不更新。
  同时确认它指向的 tools/gh_push_api.js 存在（本机访问不了 github.com，只能走 GitHub API）。

第五步：用 automation_update 创建 6 个定时任务。
  prompt 内容直接取自「A股简报-定时任务提示词.md」，每个任务的 prompt 字段粘对应那段。
  工作目录（cwds）统一设为 D:\workbuddylujing\A股推送系统
  任务清单如下，创建前先把这份清单列给我确认：

  | # | 名称 | rrule | 备注 |
  |---|---|---|---|
  | 1 | A股早报-开盘必读资讯 | FREQ=DAILY;BYHOUR=8;BYMINUTE=30 | 发微信通知 |
  | 2 | A股量价选股 | FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=15;BYMINUTE=10 | 原为14:30，有已知缺陷（盘中跑入选数被压低）故后移 |
  | 3 | A股晚报-AI读图复盘+投资日历专版 | FREQ=DAILY;BYHOUR=21;BYMINUTE=0 | 发微信通知 |
  | 4 | A股次日验证 | FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=21;BYMINUTE=30 | 不发通知 |
  | 5 | A股早报补跑看门狗 | FREQ=DAILY;BYHOUR=10;BYMINUTE=0 | 不发通知 |
  | 6 | A股晚报补跑看门狗 | FREQ=DAILY;BYHOUR=22;BYMINUTE=30 | 不发通知 |

  全部设为 ACTIVE。

第六步：凭据。
  原 .gh-token（GitHub PAT）和 Server酱 key 从建立到删除从未轮换过，历史明文进过对话记录。
  这两个凭据的「位置」你是知道的（.gh-token 在项目根，Server酱 key 在 .sct-key 或环境变量），
  但值必须换新。告诉我具体要提供什么、放到哪里，我来提供。不要复用隔离区里的旧值。

【硬性约束（完整版在重建文档里，以下几条绝对不能违反）】
- 禁止改回 workbuddy_sites_deploy 动态沙箱部署 —— 沙箱每次部署换 URL、空闲会停机，是历史上链接失效的根因
- dashboard/ 是公网站点根目录，禁止放任何临时文件
- data.js 的 reports 数组是 newest-at-bottom（旧→新），裁剪必须先 sort by date 再 slice(-7)，否则会切掉最新一条
- 个股代码必须用 node tools/check_codes.js --fix --prune 反查校验，不能凭模型记忆填写
- 改完 data.js / screener.js 必须跑 export_json（publish.sh 和 bump_version.sh 已串联此步）
- 同一文件的多处编辑必须串行执行，并行编辑有竞态

【验收标准】
- 本地预览能看到 5 个 Tab（早报 / 晚报 / 短线 / 量价 / 日历），且能回看历史日期
- 6 个定时任务全部创建成功且状态为 ACTIVE
- 隔离区原件一字未动
- 最后给我一份「恢复完成报告」：做了什么、哪些需要我提供、还有哪些没解决

【其他要求】
- 结论先行，不铺垫背景，不夸我的需求
- 需要我做决定时，给带权衡的 A/B/C 排序方案，不要开放式提问
- 遇到文档没写清的地方，直接问我，不要自己编
```

---

## 备注

- 那份 35MB 的隔离区原件**不要删**，是最后的安全网。
- 完全恢复后如果要清理，隔离区 + 备份区一共 317MB 可以释放。
- 若想从零重写而不是复制，参照「A股简报-重建文档.md」的第三部分硬性规则，但没必要 —— 原代码是完整可跑的。
