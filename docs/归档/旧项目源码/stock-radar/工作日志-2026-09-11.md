# 2026-09-11

## 项目初始化
- 创建项目 `stock-radar`（A股信息雷达），目标：采集指定股票网站 + 博主内容，做摘要与多空分歧聚合，每天≥3次推送。
- 初始化 `AGENTS.md` / `MEMORY.md`，建立 `.workbuddy/memory/` 目录。
- 当前阶段：需求定义与方案选型，尚未写代码。
- 待用户确认：网站清单、博主清单、推送渠道、部署形态、分歧粒度。

## 项目转向（重要）
- 本工作区 `stock-radar` 的第一版实现（SQLite + 图片下载 + 逐篇详情抓取）单次采集 **19分58秒**，用户明确反馈"太慢了"，并要求复用之前做过、速度快的版本。
- 已定位到备份项目 **`D:\workbuddylujing\2026-09-11-20-31-59`（stock-lens）**，采用「无后台」架构：离线批处理 + 静态文件，零第三方依赖。
- **结论：本项目后续以 stock-lens 为主体，本工作区（stock-radar）搁置。** 后续所有改动都在 `2026-09-11-20-31-59` 内进行。

## 在 stock-lens 中完成的工作
- 新建 `src/collectors/blogs.py`：博主采集（韭研公社 / 淘股吧），仅标准库。四源实测 8/5/5/6 条，耗时 0.9 秒。
- `local/update.py` 接入博主板块（`[5/5]`），完整运行 8.3 秒、5/5 成功。
- `local/web/index.html` 新增"博主观点"标签页（`renderBlogs`，4 组 24 条目卡片）。
- 定时任务从「每日 1 轮 18:10」改为「4 轮 07/12/16/22」，幂等判据从「当日」细化为「轮次」，16 条边界用例全通过。
- `local/verify.js` 断言从 18 项扩到 28 项，全通过。
- `mkschedule.py` 新增 RUN_HOURS 一致性守卫（两处定义不一致时直接报错退出）。
- 同步更新 `AGENTS.md` / `MEMORY.md` / `local/README.md` / `install_schedule.bat`。

## 关键取舍
- 博主采集**不抓详情页、不下载图片、不落库** —— 这是速度从 20 分钟降到 0.9 秒的根本原因。
- 摘要目录剥离只做「开头编号目录」这一件确定性高的事，不再剥正文小标题（试过，会丢正文，已回退）。
