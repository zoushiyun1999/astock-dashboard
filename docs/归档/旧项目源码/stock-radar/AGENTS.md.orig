# AGENTS.md

> 项目级 Agent 工作约束。任何"干活"类请求开始前必读。

## 项目标识

- **项目名**：stock-radar（A股信息雷达）
- **根目录**：D:\workbuddylujing\2026-09-11-22-13-32
- **创建日期**：2026-09-11
- **一句话目标**：采集指定股票网站 + 指定博主的内容，做摘要与「多空分歧」聚合，通过 Web 前端每天至少 3 次定时推送给用户。

## 技术栈

- 语言/运行时：Python 3.13（采集与聚合）/ 前端待定（建议 Next.js 或纯静态 + API）
- 关键依赖：待定（采集层 httpx/playwright；解析层 trafilatura；存储 SQLite）
- 构建/运行命令：待定

## 工作约束

1. 结论先行，再给理由；不铺垫背景。
2. 默认中文；代码、命令、变量名用英文。
3. 方案有问题直接指出，不谄媚、不夸需求。
4. 需要选择时给 A/B/C 排序方案 + 权衡，不问"你确定吗"。
5. 改动前先读相关文件，不凭猜测编辑。
6. 破坏性操作（删除、重装、覆盖）必须先说明影响并等确认。
7. **合规红线**：只采集公开可访问内容；遵守 robots.txt 与平台 ToS；不绕过登录墙/付费墙；不存储个人信息；输出为摘要与观点聚合，不整篇转载。

## 目录约定

```
stock-radar/
├── AGENTS.md
├── MEMORY.md
├── .workbuddy/memory/        # 工作日志（agents 自动维护）
├── config/
│   ├── sources.yaml          # 站点 & 博主清单
│   └── settings.example.yaml # 配置模板（不含密钥）
├── src/
│   ├── collectors/           # 各站点采集器
│   ├── parsers/              # 正文抽取 & 清洗
│   ├── summarize/            # 摘要 & 分歧聚类
│   ├── store/                # DB schema / 去重
│   └── app/                  # Web 前端 + API
├── data/                     # SQLite、原始快照（gitignore）
└── scripts/                  # 定时任务入口
```

## 禁止事项

- 不提交凭据、密钥、token 到仓库；只在 MEMORY.md 记录"位置"，不记录"值"。
- 不擅自引入新框架/大依赖，先提方案。
- 不动 `.workbuddy/` 目录。

## 变更流程

先改文档（AGENTS.md / MEMORY.md），再改实践，不反过来。
