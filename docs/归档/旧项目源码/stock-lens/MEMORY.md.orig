# MEMORY.md

> 项目长期记忆。工作中学到新东西主动回写，不等提醒。
> 凭据只记位置不记值；代码里能查到的东西不往这里复制。

## 项目快照

- **项目名**：stock-lens（个人股票分析站）<!-- 推断值，待确认 -->
- **当前阶段**：本地取数（行情 4 板块 + 博主 4 源，5/5）+ 边缘托管均已实测通过
- **最后更新**：2026-09-11

## 架构决策记录（ADR）

| 日期 | 决策 | 原因 | 放弃的方案 |
| --- | --- | --- | --- |
| 2026-09-11 | 数据层不自建，复用 westock-mcp + tdx-connector | 行情/财务/板块/条件选股已被现成连接器覆盖，自建等于重复投入且要持续维护数据管道 | 自建 akshare/tushare 采集与清洗管道 |
| 2026-09-11 | 采集默认走"手动投喂 / RSS"，网页抓取仅作增强项 | 公众号类无官方接口、抓取脆弱且违反服务条款；把最不稳定的部分降级为可选，避免整个项目被一个环节拖死 | 全自动模拟客户端抓取作为主路径 |
| 2026-09-11 | 候选基线方案：本机单体（FastAPI + SQLite + APScheduler + 静态前端） | 个人自用无需高可用；本机零现金成本、零备案、延迟最低；Tailscale 解决远程访问 | 前后端分离 + 云托管（作为备选 B） |
| 2026-09-11 | 摘要任务固定排在 A 股收盘后（18:00 之后）执行 | 业务上需要当日完整数据；同时落入 DeepSeek 空闲时段（高峰为工作日 9-12、14-18），API 成本减半 | 盘中实时摘要 |
| 2026-09-11 | **雪球不做自动采集，只做手动投喂** | 雪球服务条款明文禁止把其内容用于 AI 系统（含 RAG）、禁止创建含其内容的归档/缓存数据集。用户方案三项全命中；且它是三站中反爬最强、维护成本最高的 | 自动抓取雪球博主内容 |
| 2026-09-11 | 采集按站点能力分三层：全自动 / 半自动 / 手动 | 三站横跨三种技术形态（传统SSR / NuxtSSR+内部API / 纯CSR），一刀切方案必然被最难的站点拖死 | 统一采集器 |
| 2026-09-11 | **适配器抽象优先于单站实现**，目标新站接入 1–3h | 用户明确"后续还会增加站点"，若每次都要重写采集逻辑，扩展成本会失控 | 硬编码各站采集逻辑 |
| 2026-09-11 | **本地版采用「无后台」架构：离线批处理 + 静态文件**，取代 FastAPI 常驻服务 | 后台服务存在的唯一理由是"随时响应请求"；本场景数据一天更新一次，用不上。换来零进程、零端口、零网络暴露，且是唯一需要 secrets 的地方（脚本进程） | FastAPI + APScheduler 常驻服务（作为方案 A 的原始形态，已简化） |
| 2026-09-11 | 前端数据以 `window.DATA = {…}` 写入 `data.js`，用 `<script src>` 加载 | `file://` 下 `fetch('data.json')` 被同源策略拦截（opaque origin）；经典 script 加载不受限。这是"双击 HTML 即用"的前提 | 本地起 http server 提供 json |
| 2026-09-11 | **需求确认「外出也要看」→ 主用链路转为阿里云服务器**，本地版保留作开发与备用 | 服务器取数与本地取数是同一个脚本（CLI 无凭证依赖，已实测），迁移成本近零；且不必让本机常开 | 仅本地部署；内网穿透（外出时家里电脑未必开着，与本需求相冲） |
| 2026-09-11 | 远程访问方式选 **IP:端口 + Basic 认证**，不绑域名 | 备案约束的是"域名解析到境内服务器"，用 IP:端口 不涉及域名解析，无需备案。个人只读页面不需要 HTTPS | 域名+备案（1–3周，没必要）；Tailscale（更干净但多一层依赖，作为进阶选项保留） |
| 2026-09-11 | **不为了免备案而改用境外地域**，保持国内地域 | 免备案省掉的是一个"本来就不需要做的事"（不绑域名即无备案义务）；而换境外要付出真实代价：服务器在境外访问国内数据源（westock API、tgb.cn、jiuyangongshe.com）跨境绕行 + 可能的地域风控 —— 这是系统每天必须成功的环节。且阿里云轻量不支持改地域，只能退款重买，价格涨 5–10 倍 | 中国香港/境外地域（仅当明确需要"域名+不备案"时才考虑） |
| 2026-09-11 | **用户已有域名、且不愿买服务器 → 转向「无服务器静态托管」路线**：托管用腾讯云 EdgeOne Makers，取数仍在本地 | 网站产物只有 index.html + data.js 共 28KB、全相对路径引用，本来就适合静态托管。关键是拆分辨识：**「网页托管」可以无服务器，「取数执行」不行** —— 后者必须有个能访问国内数据源的运行环境。备案只跟前者有关，取数环境不需要域名也不需要公网暴露 | 阿里云轻量服务器（用户明确不想再买）；纯本机（外出访问不便） |
| 2026-09-11 | **定时更新用「错过则开机补跑」，不要求本机常开**：计划任务走 XML 定义开启 `StartWhenAvailable`，配合 `update.py` 的当日幂等检查 | 用户明确「电脑可以常开但会关机，不可能 24 小时开」。关键判断：取数只需每天一次，所以"今天没跑"和"从没跑过"是同一件事 —— 补跑即可覆盖，不需要一台常驻设备 | 要求本机 7×24；云函数定时（成本反而更高，见踩坑） |
| 2026-09-11 | **托管定为 EdgeOne Makers 免费版**，取数仍在本机，用 `publish.bat` 一键发布 | 免费版配额远超个人需求（Cloud Functions 100万次/月、自定义域名 200 个、构建 500 次/月）；用户域名未备案，而平台分配的 `*.edgeone.cool` 子域名免备案，**实测国内访问首字节 0.3–0.5s** | 阿里云 OSS 国内绑域名必须备案；Vercel 国内不可访问 |
| 2026-09-11 | **博主采集的载体是「列表接口 + 标题/摘要」，不抓详情页、不下载图片** | 需求是"看博主说了什么、点进去看原文"，不是镜像全文。抓详情页要浏览器且 20 分钟量级；只读列表接口 0.9 秒完成。图片留在原站，前端点击跳转即可 —— 这一条直接把单次采集从 20 分钟压到 9 秒 | 下载正文+图片做站内阅读（第一版这么做的，被用户否掉） |
| 2026-09-11 | **定时任务改为每天 4 轮（07/12/16/22），幂等判据从「当日」细化为「轮次」** | 需求是"每天至少三次"+"晚报 22:00 出"。原「当日幂等」会导致早报跑完后，中午/收盘/晚报全部被跳过 | 单轮 18:10 + 当日幂等（早报需求无法满足） |
| 2026-09-11 | **摘要里的目录剥离只做「开头编号目录」这一件确定性高的事，不再剥正文小标题** | 试过继续剥"一、盘前资讯 1、要闻简讯"类小标题，结果投资日历的正常开头被连带削掉、早报正文被 cut 一半。润色收益不稳，风险却是丢内容。摘要的定位只是"让人决定要不要点原文" | 多层正则反复剥离（越修越复杂，且会静默丢正文） |

## 环境事实

- 工作区：Windows（win32），默认 shell 为 Git Bash
- 目录名为时间戳 `2026-09-11-20-31-59`，无语义，项目名靠约定
- 已连接连接器：westock-mcp、tdx-connector、agent-mail
- **用户已有域名，但未备案**（2026-09-11 确认）→ 只能走境外节点或平台子域名
- **不购买服务器**；原「已购买阿里云轻量 2核2G」是误读（用户原话是假设句式「如果我已买好服务器」），已于 21:38 更正
- **本机不能 7×24 开机**（用户原话：可以常开，但会关机）→ 这是「开机补跑」方案的前提
- 无家用常开设备（NAS / 旧电脑 / 树莓派）—— 因此取数只能靠本机或云端
- 阿里云轻量 2核2G 容量评估（仅在将来改主意时参考）：单用户 IO 密集负载，常态内存 400–600MB 充裕；唯一风险是 Playwright（150–400MB/实例），对策为串行化采集 + 2GB swap
- 宿主会给 HTML 文件注入 `data-page-node-id` 属性 —— 编辑既有 HTML 前必须先 Read 取当前内容，不能凭上次写入的内容做字符串匹配

## 踩坑与结论

<!-- 现象 → 根因 → 解法。避免重复踩 -->

- **选股 CLI 无凭证依赖 → 可脱离 WorkBuddy 环境独立部署**。现象：`env -i`（清空环境变量）下运行 `scripts/index.js filter --preset LowPE` 仍正常返回真实数据。结论：不需要任何本地配置或 token 文件，方案 B（独立服务器部署）成立。
- **数据服务会不定时抖动 → 选股结果必须落库留档**。现象：2026-09-11 21:10，`strategy macd_golden` 与 `label risk_st` 同时返回 `[SKILL_003] error_type=2 suggest=3 msg=service error`，而 `filter` 正常。根因：对方服务端问题，不由我们控制。解法：每天跑完写本地库，前端优先读本地，取数失败时降级展示上次存档并标注日期 —— 不要假设失败后重跑即可恢复。
- **编辑本仓库的 HTML 文档前必须先 Read**。现象：Write 后用上次写入的内容做 Edit 匹配会失败。根因：宿主向 HTML 注入 `data-page-node-id` 属性，磁盘内容与写入内容不一致。
- **三站侦察结论（2026-09-11，21:14–21:30）**：
  - 淘股吧 `tgb.cn` —— 传统 SSR，无前端框架标记。博主页 `/blog/{数字ID}`，正文 `/a/{短码}`。robots 仅禁 `/view?topicID=*`、`/Reply/*`、`/new/ngb/*`、`/new/dav/*`；**`/a/` 未被禁止 → httpx 直取，不需要浏览器**。
  - 韭研公社 `jiuyangongshe.com`（= `jiucaigongshe.com`）—— Nuxt SSR。**列表页正文在 HTML 中，但文章详情页正文不在**（实测仅 481 中文字符，只有标题+免责声明）→ 详情需浏览器。robots 为 `Disallow:` 空值，实质全站允许，有 sitemap。内部 API 基址 `https://web-api.jiuyangongshe.com`（130 个端点，含 `/api/v1/user/newest-article-tool`），但直接 GET 返回 **502**（网关在校验 App 侧请求头/签名）；站内含付费内容体系（`/api/v1/article/read/pay`）。**未尝试伪造签名绕过。**
  - 雪球 `xueqiu.com` —— 纯 CSR（首页 HTML 内 0 个中文字符）+ robots 明文禁止 AI 使用 + `Disallow: /*.json` 封禁全部 JSON 接口。
- **`/tmp` 在 Git Bash 各次调用间不持久** → 临时文件写到工作区目录（如 `.scratch/`）。
- **`filter --preset` 每个预设返回的指标列不同** → 不能硬编码表格列。实测：`LowPE`→`PE_TTM/PE_Fwd/PB`，`HighDividend`→`DividendRatioTTM`，`HighROE`→`ROEWeighted`，`MainInflow`→`MainNetFlow/JumboNetFlow/BlockNetFlow/TurnoverValue`。解法：脚本保留「该预设自己的所有数值字段」，前端按优先级动态生成列。
- **数值单位陷阱（已踩，务必记住）**：
  - `westock quote` 的 `total_market_cap` 单位是**亿元**（茅台 15940.54 = 1.59 万亿），不是元。按元处理会丢单位。
  - `filter --preset MainInflow` 的 `MainNetFlow` 等资金字段单位是**元**（3166303181 = 31.66 亿），需按 亿/万 缩放。
  - 判断方法：结合价格与股本反推，别凭字段名猜。
- **数据源故障是「部分可用」而非「全挂」** → 降级必须做在单项粒度。实测 2026-09-11：`filter` 正常，同时 `strategy` / `ranking CompScore` / `market-overview` 返回 `service error` 或空。`ranking CompScore` 无 `--raw` 时输出"无数据"、加 `--raw` 时输出 `null` —— 两种都是失败，不能把 `null` 当成功。
- **手机端响应式：`minmax(340px,1fr)` 在 375px 屏上必然横向溢出** → 用 `minmax(min(340px,100%),1fr)`，让下限不超过容器宽度。窄屏还要隐藏 `.cd`（股票代码）——那是最大的一块冗余宽度。
- **`sudo -u <user> cmd` 默认不切换 `HOME`**（sudo 只有在 `always_set_home` 开启时才改）。后果：以目标用户身份装东西会装进调用者的 HOME。必须用 `sudo -H -u <user>`。
- **Ubuntu 自带的 `nodejs` 包版本过低**（22.04 是 12.x，24.04 是 18.x），低于本 CLI 要求的 Node ≥18 → 服务器上必须走 NodeSource 装 Node 20，不能 `apt install nodejs` 了事。
- **本机 CLI 可运行 ≠ 服务器 IP 可运行**。服务器是另一个出口 IP，对方服务是否对云厂商 IP 有风控，只能在服务器上实测。部署脚本已把"实测取数"作为最后一步。
- **ICP 备案的触发条件是「域名解析指向中国内地服务器」，不是「有服务器」** → 纯 IP:端口 访问不触发备案；服务器在境外（含中国香港）则永不触发。官方口径补充：托管于非中国内地服务器的网站"无需工信部备案但需完成公安联网备案"（实操中对纯 IP 自用工具基本不涉及，且不影响访问）。
- **阿里云轻量有「通用型」与「国际型」两个规格族，线路不同，易混淆** → 规格 ID 带 `i1` 后缀的是**国际型**，官方文档明写"采用 BGP(非中国优化)公网线路，服务中国内地用户有较高的公网时延与丢包率，仅适用于不需要对中国内地用户提供服务的应用场景"。**通用型的地域列表里同样包含中国香港/新加坡/日本**，走的是标准 BGP。若将来真要买香港，务必挑通用型，别买国际型。
- **更正早前关于"海外轻量流量包陷阱"的说法**：2026 年阿里云轻量在售规格多数已是"无固定流量（不收取流量费用）"，不存在超额天价账单。香港 1 元/GB 的超额单价只针对仍含固定流量配额的**老套餐**。流量已不是选型变量。
- **阿里云轻量的地域不可变更** → 换地域 = 退款重买，且促销价不跟随（国内 2核2G 秒杀 ¥38/年、日常 ¥68/年；香港同配置估算 ¥336–658/年）。
- **备案对「不用服务器」构成本质冲突**：ICP 备案是针对**域名**的，但阿里云/腾讯云都要求先买一台中国内地节点服务器才能拿到「备案服务码」（一台 ECS 最多生成 5 个）。所以「不想买服务器 + 想用国内节点 + 域名未备案」这个组合走不通，必须依赖云函数/轻量等任一国内资源。**推论：取数环境与托管环境可以共用同一个「备案服务码来源」，但取数环境本身不需要备案。**
- **阿里云 OSS 国内 Bucket 绑自定义域名必须 ICP 备案**，且官方明确：用 Bucket 默认域名访问 HTML，浏览器会**强制下载而非预览** → 想正常浏览必须绑自定义域名，也就必须备案。非中国内地 Bucket（中国香港/新加坡）绑域名免备案，但官方提示"中国大陆用户访问境外节点时跨境网络延迟可能增大"。
- **静态托管平台在国内的实际可用性差异极大（2026-09 核实）**：Vercel 国内**无法访问**；GitHub Pages 国内极慢；Cloudflare Pages/Workers 联通晚高峰 800ms+ 且 pages.dev/workers.dev 自 2022 年起常遭 DNS 污染；**腾讯云 EdgeOne 最优**。
- **【关键区分】EdgeOne 里「用自己的域名」和「用平台子域名」是两回事**：用自己的域名时，未备案只能走境外节点（估算 670–1400ms）——因为备案义务由"域名解析指向境内服务器"触发；用平台分配的 `*.edgeone.cool` 子域名时，备案义务在平台，**实测国内首字节 0.32–0.50s**。→ 所以「未备案」不等于「一定慢」，取决于用谁的域名。这是本轮最重要的技术澄清。
- **【重要更正】腾讯云 SCF 的「免费额度」已经取消 —— 不能用"云函数免费"来论证方案**。官方计费文档明确：开通前三个月有免费试用，**三个月后不再有免费额度，系统每月自动扣 12.8 元基础套餐费**（按天扣 0.41 元）。即 ¥153.6/年，**比轻量服务器 ¥99/年（同价续费档）还贵**。第三方文章里"每月 100 万次免费"的说法是过时信息。唯一免除条件：上个自然月调用量/资源/外网流量**均为 0**。→ 结论：拿云函数替代服务器省钱是伪命题。
- **EdgeOne Makers 免费版配额（2026-09 实测官方文档 product/1552/132789）**：项目 40 个、构建 500 次/月、Edge Functions 300 万次/月（代码包 5MB）、**Cloud Functions 100 万次/月（代码包 128MB，最大执行 30 秒可配至 120 秒）**、Agents 20 万次/月（单请求最长 3600 秒）、KV 1GB、Blob 1GB、**自定义域名 200 个 + 免费 SSL**、内置模型 50 万 token/月。免费版不承诺 SLA。平台模板含"定时任务"场景。
- **EdgeOne 可以匿名部署（免登录），是验证托管方案的最低成本手段**：`npx edgeone makers deploy ./web --anonymous --site china --json`。实测结果：拿到 `*.edgeone.cool` 平台子域名，**未备案可用**，国内访问 index.html 首字节 0.32–0.50s、data.js 0.31–0.52s（远好于海外节点 670–1400ms 的估算）。注意匿名部署 **1 小时后过期**，需用返回的 `claimCommand` 或 claimUrl 登录认领才能转正。
- **`schtasks /Create` 命令行没有「错过计划时间后补跑」这个开关**。对应设置 `StartWhenAvailable` **只存在于 XML 定义与 PowerShell 两种途径**。要开机补跑只能走 XML（本仓库用 `local/mkschedule.py` 生成，**必须是 UTF-16 编码**，schtasks 拒收 UTF-8 定义文件）。
- **Bash 沙箱把 `schtasks.exe` 列入黑名单**（"PROGRAM BLOCKED BY SECURITY POLICY"），且该阻止无法通过提权绕过。替代路径：用 PowerShell 的 `Register-ScheduledTask` cmdlet（不走该 exe）。注意 `schtasks /XML` 在无提升权限时也会返回"拒绝访问"。
- **PowerShell 工具（本环境）不回传 stdout**，无论命令成功与否都只返回 "Command completed with exit code 0"。需要看输出时：把结果 `Set-Content` 落到工作区文件，再用 Read 读回。
- **任务主体标识用 `USERDOMAIN\USERNAME`，不要用 `COMPUTERNAME`**：Git Bash 不导出 `COMPUTERNAME`（值为空），但 `USERDOMAIN` 有值（实测 `DESKTOP-NQT2JG0\zoush`）。XML 里缺 UserId 会导入失败。

- **博主采集 ADR（2026-09-11，实测四源全部免登录可抓）**：
  - **韭研公社 `/u/{user_id}`** —— Nuxt SSR，数据内联在 `window.__NUXT__`，且是**压缩后的 IIFE**：字段名可能被替换成变量。
    **解法：按 `title:"..."` 锚点切区块，块内独立取字段。**
    踩过的坑：最初用 `article_id` 作锚点向前取字段 → 跨条目串字段（标题对但正文/股票/链接全错，正文长度为 0）。实测修正后 0 条 → 15 条正确解析。
    另一个坑：`article_id` 在 `title`/`content` **之后**，且是短 ID `[a-z0-9]{8,32}`，不是 32 位哈希（我最初按哈希猜，匹配不到）。
  - **韭研公社 user_id 拿不到时**：昵称 `A股投资日历` 的 `user_id` 被压缩成变量 `g` → **解析 IIFE 尾部实参表** `}(0,null,"",1,false,"70phqzs09w","648089de4f1649de9e18b8285faca8b8",...)` 解出 `g` 的值。
  - **韭研公社首页时间轴抓不到**：事件是 JS 异步加载，静态 HTML 里没有 → 抓博主帖子（这正是需求要的）。
  - **淘股吧 `/blog/{blog_id}`** —— 纯 SSR 免登录直出，但**列表按时间正序**（抓到的是 8 月旧文）。
    **解法：从标题解析日期后倒序取最新**（`_tgb_infer_date` 带合理性校验，避免把股票代码当日期；跨年用"推断日期比今天晚 30 天以上则视为去年"）。
  - **淘股吧翻页接口需登录**：`/user/blog/moreTopic` 返回登录页 → 放弃翻页，单页全抓 + 日期排序。
  - **淘股吧图片懒加载**：`src` 是 `placeHolder.png`，真址在 `data-original`。**本项目不下载图片，此坑不影响**（记录备查，若将来要抓图需注意）。
  - **淘股吧正文边界标记**：`<!-- 设置播放器容器 -->` 是正文**中间**注释而非结束标记，用它截断会导致 11 张图只取到 5 张 → 改用尾部区块注释（`<!--打赏` / `<!--相关` / `<!--评论`）作边界。（同样因不抓详情页而暂不涉及）

- **摘要目录剥离的实现要点**：目录条目名长短不一（"要闻简讯" 4 字 vs "盘前个股人气热度" 8 字），**定长正则必然切歪**。可靠做法是找"从 1 开始连续递增的编号序列"（`_JY_NUM`），序列断档处即目录末尾。切割点取**最后一个编号的起始位置** —— 宁可留下末尾那条条目名（"12、调研点评"），也绝不多切一个字。判据 `want >= 5` 才动手，避免把正文里的单个"1、"误当目录。

- **性能对比（同一需求的两版实现，是本轮最重要的事实）**：第一版（`2026-09-11-22-13-32`，SQLite + 图片下载 + 逐篇详情抓取）单次采集 **19 分 58 秒**；stock-lens 版（只读列表接口、不落库、不下图）博主采集 **0.9 秒**、完整 `update.py` **8.5 秒**。**用户明确反馈"你这样太慢了"并要求复用 stock-lens 架构** —— 这是选型的决定性依据。

## 用户偏好与纠正

- 拒绝谄媚：不要"这是个好问题"式开场，不要夸需求。
- 需要选择时给 A/B/C 方案 + 权衡，不要开放式提问。
- 偏好简洁清爽的 UI。
- 有既定习惯：每个项目根目录维护 AGENTS.md + MEMORY.md（模板源 `~/.codex/templates/`）。
- 在做一个"A股推送系统"，与本项目可能同源，涉及推送时先确认二者关系。

## 外部资源位置

| 资源 | 位置 | 备注 |
| --- | --- | --- |
| 项目约束/记忆模板 | `~/.codex/templates/` | AGENTS.md、MEMORY.md |
| 用户身份/画像 | `~/.workbuddy/USER.md`、`IDENTITY.md`、`SOUL.md` | 会话自动注入 |
| 选股 CLI（westock-tool） | `~/.workbuddy/plugins/cache/cb_teams_marketplace/finance-data/1.5.0/skills/westock-tool/scripts/index.js` | 3.1MB 单文件打包，Node ≥18，免 npm install，已实测可用 |
| **行情 CLI（westock Go）** | `~/.local/bin/westock.exe`（全局安装，非单文件包） | `quote`（个股行情，Markdown 表）、`hot sector`（热搜板块）、`sector ranking`（行业榜）、`kline`/`technical`/`chip`/`fund` 等。**无 JSON 输出开关**，需解析 Markdown 表 |
| **本地版（无后台）** | `local/` | `update.py`（取数）+ `mkschedule.py`（生成任务XML）+ `install_schedule.bat`（注册补跑任务）+ `publish.bat`（取数并发布）+ `web/index.html` + `config.json`。见 `local/README.md` |
| **EdgeOne CLI 关键参数**（实测 v1.6.39） | `npx edgeone makers <cmd>` | 注意 `edgeone pages` 已废弃，现为 `makers`。`deploy ./dir -n <项目名>`；`-t/--token` 支持非交互 CI 部署；`--anonymous --site china\|global --json` 免登录部署（**1 小时过期**，用返回的 `claimCommand` 认领）；`--area` 取 `global`（含国内节点，需备案）或 `overseas`。`login --site china` 首次登录 |
| **部署到服务器**（备选存档） | `deploy/` | `push.sh` + `setup-server.sh` + nginx 配置。**当前不走这条路**（用户不买服务器），保留备将来需要备案或常驻服务时启用 |
| 方案文档 | `docs/stock-lens-技术方案与成本估算.html` | 四方案对比 + 成本明细 + 服务器评估 + 站点侦察 + 本地无后台（十四节）+ 云端部署（十五节） |
| **采集站点侦察笔记** | `docs/采集站点侦察笔记.md` | robots 原文、URL 结构、韭研公社 130 个 API 端点、适配器配置骨架 |
| SSR/CSR 判定脚本 | `.scratch/analyze.py` | 判断目标站内容是否在 HTML 中（决定要不要上 Playwright） |

## 待办 / 悬置问题

**已闭环**

- [x] 采集站点 → 三站：jiuyangongshe.com、tgb.cn、xueqiu.com（后续会增）
- [x] 本地版（无后台）→ 已实现并验证，产物在 `local/`
- [x] 部署路线 → **无服务器静态托管**：EdgeOne Makers 托管 + 本机取数
- [x] 域名备案状态 → **未备案**（已确认）；家用常开设备 → **无**（已确认）
- [x] 云函数跑取数可行性 → 已评估，**不划算**：SCF 三个月后强制 ¥12.8/月（¥153.6/年）> 轻量服务器 ¥99/年
- [x] 本机不能 7×24 → 已用「计划任务 + 开机补跑 + 当日幂等」覆盖，并已实测
- [x] AGENTS.md 技术栈字段 → 已补
- [x] **四个博主源已确认并实现**：早报=韭研公社「开盘必读」、晚报=淘股吧「湖南人」「shenghuo329」、投资日历=韭研公社「A股投资日历」
- [x] 博主采集模块 → `src/collectors/blogs.py`，四源实测 8/5/5/6 条，0.9 秒
- [x] 博主观点前端标签页 → `local/web/index.html`，`verify.js` 28 项断言全通过
- [x] 每日 4 轮定时 → `RUN_HOURS=[7,12,16,22]`，幂等改为按轮次判定，16 条边界用例全通过

**待用户确认 / 待做**

- [ ] **首次发布需用户登录一次 EdgeOne**：`npx edgeone login --site china`（我无法代做）
- [ ] 是否把自有域名绑到 EdgeOne —— 未备案则只能走境外节点；也可继续用平台子域名（实测更快）
- [ ] 确认是否已有 LLM API Key（无则需开通，首充 ¥50 够用大半年）
- [ ] 确认自动选股的技术条件 2–3 条（注意 `strategy`/`ranking` 近期在报 service error，可能只能用 `filter --preset`）
- [ ] 项目名 stock-lens 是否可用（当前为推断值）
- [ ] 把 `local/config.json` 的 `watchlist` / `picks` 换成真实关注内容（当前是示例值）
- [ ] 淘股吧 `shenghuo329` 最新一篇停在前一天（9/10 22:00）—— 待观察是当日未发还是抓取问题
- [ ] 【可选】把每日取数也搬到 EdgeOne Cloud Functions，彻底去掉"本机需开机"。免费额度够（100 万次/月），但需实测两点：① 函数内能否跑 westock CLI ② 函数出口 IP 是否被数据源风控
