# AGENTS.md

> 项目级 Agent 工作约束。任何"干活"类请求开始前必读。

## 项目标识

- **项目名**：stock-lens（个人股票分析站）<!-- 推断值，待确认 -->
- **根目录**：D:\workbuddylujing\2026-09-11-20-31-59
- **创建日期**：2026-09-11
- **一句话目标**：个人自用的股票分析站——采集指定博主的分析内容并摘要、按技术条件自动选股、跟踪热点板块与个股。

## 技术栈

- 语言/运行时：Python 3.13（取数脚本）、原生 HTML/CSS/JS（前端）
- 关键依赖：**无第三方依赖** —— Python 只用标准库；前端不引任何 CDN
- 运行命令：`cd local && python update.py`（Windows 双击 `update.bat`）
- 发布命令：`cd local && publish.bat`（取数 + 发布到 EdgeOne Makers，需先 `npx edgeone login --site china`）
- 定时任务：`cd local && install_schedule.bat`（每天 **07:00 / 12:00 / 16:00 / 22:00 四轮**，错过后开机补跑）
- 自检命令：`cd local && node verify.js`（28 项前端渲染断言）
- 现状：本地取数（5/5 板块）+ 博主采集 + 边缘托管均实测通过

## 架构约束（重要）

本项目本地版采用**无后台**架构，以下约束不要违反：

1. **不引入常驻服务**（FastAPI/Flask/n8n 等）。取数逻辑写成"跑完即退"的脚本。
2. **前端数据必须用 `<script src="data.js">` 加载**，不能用 `fetch('*.json')` ——
   `file://` 下会被同源策略拦截，页面会白屏。
3. **前端不引 CDN**（Tailwind/Chart.js 等），否则断网即不可用。
4. **取数必须做单项降级**：某项失败要回落上次留档并标注日期，不能整批失败。
5. **取数脚本必须保持「按轮次幂等」**：同一轮次已成功取过数时应直接退出（`--force` 可绕过）。
   计划任务开了「错过补跑」，开机瞬间可能连续触发；破坏幂等会导致重复打第三方接口，
   并让"看到的是不是本轮数据"判断失真。
   - 轮次表定义在 `update.py` 的 `RUN_HOURS`，**必须与 `mkschedule.py` 的 `RUN_HOURS` 一致**。
   - 判据是"上次成功是否落在当前轮次窗口内"（`_slot_of_stamp(stamp) == slot`），
     用 `==` 而非 `>=`：早报跑完不能让中午那轮也跳过。
   - 02:00–06:59 为"深夜补跑"窗口：昨日任一轮成功即视为收工，避免开机时把前一天的
     四轮依次全量补跑。
6. **定时任务定义改走 `mkschedule.py`**，不要直接用 `schtasks /Create` 命令行 ——
   后者的开关里没有 `StartWhenAvailable`，会静默丢掉"关机错过则开机补跑"这一行为。

## 采集数据源（已实现）

| 板块 | 站点 | 标识 | 频率 | 备注 |
| --- | --- | --- | --- | --- |
| 早报 · 开盘必读 | 韭研公社 | user_id `df07647c…c08f3` | 每交易日 07:00 后 | Nuxt SSR，需剥目录 |
| 晚报 · 湖南人 | 淘股吧 | blog_id `444409` | 每晚 22:00 后 | 纯 SSR，按标题日期倒序 |
| 晚报 · shenghuo329 | 淘股吧 | blog_id `563404` | 每晚 22:00 后 | 同上 |
| 投资日历 | 韭研公社 | user_id `648089de…ca8b8` | 变更驱动 | 不定期更新，抓最新即可 |

采集模块：`src/collectors/blogs.py`，**只用标准库**，零第三方依赖。
关键坑与解法见 MEMORY.md"博主采集 ADR"一节。

## 已有外部数据能力（重要，勿重复造）

| 能力 | 载体 | 覆盖范围 |
| --- | --- | --- |
| 行情/财务/板块/资金流/选股器 | westock-mcp（已连接） | A股/港股/美股，含条件选股、排行榜、涨停、龙虎榜 |
| 通达信本地行情与选股 | tdx-connector（已连接） | 本地 TDX 数据，含筛选与研究 |
| 邮件 | agent-mail（已连接） | 可用于定时推送日报 |

## 工作约束

1. 结论先行，再给理由；不铺垫背景。
2. 默认中文；代码、命令、变量名用英文。
3. 方案有问题直接指出，不谄媚、不夸需求。
4. 需要选择时给 A/B/C 排序方案 + 权衡，不问"你确定吗"。
5. 改动前先读相关文件，不凭猜测编辑。
6. 破坏性操作（删除、重装、覆盖）必须先说明影响并等确认。
7. 采集类功能必须注明目标站的服务条款与反爬风险，不默认"能爬"。

## 目录约定

```
D:\workbuddylujing\2026-09-11-20-31-59\
├── AGENTS.md          # 本文件
├── MEMORY.md          # 项目长期记忆
├── docs/              # 方案文档、侦察笔记
├── local/             # 本地版（无后台）：取数脚本 + 静态前端
│   ├── config.json    #   配置：选股预设 / 板块数 / 自选股 / blogs 源清单
│   ├── update.py      #   取数脚本（跑完即退，含按轮次幂等；RUN_HOURS 定义于此）
│   ├── mkschedule.py  #   生成计划任务 XML（4 轮触发；RUN_HOURS 须与 update.py 一致）
│   ├── install_schedule.bat  # 注册每日 4 轮任务
│   ├── publish.bat    #   取数 + 发布到 EdgeOne Makers
│   ├── verify.js      #   前端渲染自检（Node 最小 DOM 桩，28 项断言）
│   ├── logs/          #   分槽日志 slot0..slot3.log（按轮次拆分，便于定位）
│   ├── web/           #   纯静态前端（index.html + 生成的 data.js）
│   └── README.md      #   使用说明
├── deploy/            # 服务器部署方案（备选存档，当前不走）
├── src/
│   └── collectors/
│       └── blogs.py   #   博主采集（韭研公社 / 淘股吧），仅标准库
└── .workbuddy/        # 工具目录，禁止改动
```

## 禁止事项

- 不提交凭据、密钥、token 到仓库；只在 MEMORY.md 记录"位置"，不记录"值"。
- 不擅自引入新框架/大依赖，先提方案。
- 不动 `.workbuddy/` 目录。
- 不违反目标网站服务条款做高频抓取；采集频率默认 ≤ 每 30 分钟一次。

## 变更流程

先改文档（AGENTS.md / MEMORY.md），再改实践，不反过来。
