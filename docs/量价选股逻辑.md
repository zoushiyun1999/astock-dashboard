# 量价选股逻辑（可编辑）

> 最后更新：2026-09-02 · 脚本 `tools/screener.js` · 定时任务 `automation-1788187841979`（周一至周五 14:30）

纯技术面条件筛选，与「📌 短线关注」（题材游资线）完全独立。**看板第五个 Tab「🔍 量价选股」**。

---

## 一、筛选条件（当前值）

| # | 条件 | 当前阈值 | 脚本里的变量 |
|---|---|---|---|
| 1 | 涨幅 | 2.5% ~ 7% | `gainMin` / `gainMax` |
| 2 | 换手率 | 2.5% ~ 20% | `turnMin` / `turnMax` |
| 3 | 总市值 | 20亿 ~ 500亿 | `capMin` / `capMax` |
| 4 | 量比 | > 1 | `volRatioMin` |
| 5 | 连续收阳 | ≥ 2 天 | `yangMin` |
| 6 | 分时均线 | 全天 ≥95% 时间在均线上 **且** 收盘价在均线上 | `aboveRateMin` |
| 7 | 排除 | 创业板(300/301)、科创板(688)、北交所(8/4开头)、ST、退市 | `passBase()` 里的正则 |
| 8 | 最多保留 | 40 只（按涨幅排序） | `maxHold` |

**改法**：直接编辑 `tools/screener.js` 顶部的 `CFG` 常量，下次运行即生效。
**试跑不写文件**：`node tools/screener.js --dry`

---

## 二、数据源（已验证可用，含踩坑记录）

| 数据 | 接口 | 说明 |
|---|---|---|
| 涨幅 / 换手 / 市值 / 量比 / 代码 | `push2delay.eastmoney.com/api/qt/clist/get` | 延迟 15 分钟，收盘后跑无影响 |
| 连续收阳（日K） | 新浪 `money.finance.sina.com.cn/.../getKLineData` | **主源，稳定** |
| 连续收阳（备用） | 腾讯 `web.ifzq.gtimg.cn/appstock/app/fqkline/get` | 高频请求会触发 WAF 拦截，仅作备用 |
| 分时均线 | `push2delay.eastmoney.com/api/qt/stock/trends2/get` | 返回每分钟价格(f53) + 均价(f58) |

### ⚠️ 已踩过的坑（改脚本时注意）

1. **`push2.eastmoney.com` 和 `push2his.eastmoney.com` 在本机网络下不通**（连接失败/返回空），必须用 `push2delay.eastmoney.com`
2. **腾讯 K 线接口会被 WAF 拦截**——跑几十次后返回 HTML 风控页而非 JSON。脚本已内置检测（响应含 `<!DOCTYPE` 或 `waf.tencent` 即判定失败并切换备用源）
3. 高频请求会限流，脚本并发控制在 4，失败自动重试 2 次（约 10 只左右可能因网络抖动失败，属正常）

---

## 三、执行流程

```
① 拉沪深主板全市场（3485 只）
        ↓  一次分页请求，约 1 秒
② 基础条件过滤（涨幅/换手/市值/量比/排除）
        ↓  约 179 只
③ 逐只查日K → 连续收阳 ≥2 天
        ↓  约 84 只
④ 逐只查分时 → 全天在均线上方
        ↓  约 31 只
⑤ 按涨幅排序，取前 40 只
        ↓
⑥ 标注「是否属于当日主线板块」（从 data.js 的 板块热点 提取股票名做匹配）
        ↓
⑦ 写入 data.js 顶层 screener 字段
```

**总耗时约 30-60 秒。**

---

## 四、数据结构

```json
"screener": {
  "date": "2026-08-31",
  "runAt": "2026-08-31 22:47",
  "count": 31,
  "criteria": {
    "gain": "2.5%-7%", "turnover": "2.5%-20%", "cap": "20亿-500亿",
    "volRatio": ">1", "yang": "≥2连阳",
    "aboveAvg": "≥95% 时间在分时均线上",
    "exclude": "创业板/科创板/北交所/ST/退市"
  },
  "list": [
    {
      "code": "603819", "name": "神力股份", "price": 26.13,
      "gain": 6.81, "turnover": 4.09, "volRatio": 2.04, "cap": 33,
      "yang": 5, "aboveRate": 99, "sector": ""
    }
  ]
}
```

`aboveRate` = 价格在分时均线上方的时间占比（百分比）
`sector` = 若该股属于当日主线板块则填板块名，否则为空字符串

---

## 五、前端展示

**位置**：第五个 Tab「🔍 量价选股」
**代码**：`dashboard/js/app.js` → `renderScreener()`；样式 `css/style.css` 的 `.sc-table` 等

- 顶部：日期 + 入选数量 + 运行时间
- 条件标签条：7 个筛选条件以胶囊形式展示
- 表格（可横向滑动）：`# | 名称(代码) | 涨幅 | 换手 | 市值 | 量比 | 连阳 | 均线上`
- 属于当日主线板块的个股，名称后带粉色板块标签
- 底部免责提示

---

## 六、可调参数清单

| # | 参数 | 改哪里 | 生效 |
|---|---|---|---|
| 1-7 | 全部筛选阈值 | `tools/screener.js` 顶部 `CFG` | 下次运行 |
| 8 | 最多保留只数 | 同上 `maxHold` | 下次运行 |
| 9 | 排除板块规则 | 同上 `passBase()` 的正则 | 下次运行 |
| 10 | 排序方式 | 同上 `final.sort()`（当前按涨幅降序） | 下次运行 |
| 11 | 表格列 / 配色 | `js/app.js` 的 `renderScreener()` + `css/style.css` | 需手动部署 |

---

## 七、常见调整方向

- **池子更严**：涨幅改 3%-6%、连续收阳改 ≥3 天、均线上方占比改 100%
- **只看强势**：按量比排序（`final.sort` 改为 `b.f10 - a.f10`）
- **放开市值上限**：`capMax` 改 1000（纳入大市值蓝筹）
- **加入题材维度**：把 `sector` 非空的排在前面，或直接只保留 `sector` 非空的（技术面 + 热点共振）
- **加均线/趋势条件**：需要在脚本里额外拉日K 计算 MA5/MA10/MA20，当前已拉到 10 天日K 数据可直接扩展

---

## 2026-09-22 v7 变更（选股标准优化 + 验证回路）

**筛选条件变更**（`tools/screener.js` 的 CFG）：
- 量比 `> 1` → `≥ 1.5`：量比 1.0~1.3 属正常水平，不构成"放量"证据。
- 新增高位过滤：**近 20 日累计涨幅 ≤ 25%**（防追在趋势末端，如 7 连阳股第 8 天追入）；
  上市不足 21 根 K 线的新股无法计算 → 跳过该条不拦截。K 线窗口由 10 根扩到 30 根（新浪主源/腾讯备用）。
- 漏斗从三层变四层：基础 → 连阳 ≥2 → 20日涨幅 ≤25% → 分时均线 ≥95%。

**验证回路（v7 核心）**：量价入选股此前是唯一没有实盘成绩单的模块。
- `tools/verify.js` 新增 `applyRebuildScreener`：把 `dashboard/screener.js` 历史各期入选股纳入验证，
  口径同晚报（期日的次一交易日开盘买入→收盘卖出、扣双边万五+印花千五、一字板剔除），
  标记写回各期 `list[].verify`，`by==='hist'` 幂等。21:30 的 verify cron 自动覆盖。
- `health_check` 覆盖度检查同步纳入量价股。
- 前端量价表新增「20日」「次日实盘」两列（净收益芯片，涨红跌绿；一字板/停牌/未标各有态）。
- 旧标准 8 期回补基线（2026-09-22 实测）：**105 样本、净胜率 48.6%、平均净收益 +0.36%**
  —— 略优于人气榜推荐（净胜率 42.9%、净 -0.37%），新标准（量比 1.5 + 高位过滤）能否提升以此为准绳。

**基建修复**：
- 腾讯日K改计数式请求（`day,,,N,qfq`）：日期区间式**不含当日 bar**，曾把 13 条 target=当日误标停牌；
  停牌标记不再参与幂等跳过（数据源补出该日 bar 时自动纠正）；请求间隔 150→400ms 防 WAF（HTTP 501）。
