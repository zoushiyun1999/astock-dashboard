# A股推送系统 · 恢复完成报告

> 执行日期：2026-09-12
> 目标目录：`D:\workbuddylujing\A股推送系统\`
> 隔离区原件：`D:\WorkBuddy_删除隔离_2026-09-10\WorkBuddy\A股推送系统\`（只读，**未改动**）
> 本报告为恢复会话产出，可提交入库，也可删除。

---

## 一、做了什么

| # | 事项 | 结果 |
|---|---|---|
| 1 | 读取两份重建文档 | ✅ 完整读取，未凭猜测动手 |
| 2 | 复制项目（复制而非移动） | ✅ `robocopy /E`，33M 到位；排除 5 个 `tmp_*` 残留与旧 `.gh-token` |
| 3 | 看板数据体检 `health_check.js` | ✅ 0 ERROR；6 WARN（09-01~09-03 缺 `generatedAt` 等历史字段，非本次引入） |
| 4 | 发布链 `bump_version.sh` | ✅ 四步全过：`v=202609121942` → sort_reports（无需变更）→ sync_holidays → export_json |
| 5 | 本地预览 `127.0.0.1:8899` | ✅ 5 个 Tab 全部有内容，历史日期可回看 |
| 6 | `.git/hooks/post-commit` 重建 | ✅ 从 `tools/git-hooks/post-commit` 重建 + `chmod +x`；`tools/gh_push_api.js` 存在 |
| 7 | 下线 Server酱推送通知功能 | ✅ `notify.js` / `notify_digest.js` 加禁用开关（原始实现保留，删 2 行即恢复） |
| 8 | 重建 6 个定时任务 | ✅ 全部 ACTIVE，工作目录统一 |
| 9 | 记忆回写 | ✅ 项目 `MEMORY.md` + `.workbuddy/memory/2026-09-12.md` |

### 恢复后的数据现状（真实解析，非估计）

```
reports ：7 条 · 2026-09-01 / 02 / 03 / 04 / 07 / 09 / 10   （旧→新，最新在末尾）
          ├─ 含 morning ：7 条
          └─ 含 evening ：6 条（2026-09-10 无晚报）
calendar：3 篇
screener：4 期 · 2026-09-10 / 09-08 / 09-07 / 09-04          （最新在前）
git     ：102 次提交，分支 main
```

两个历史缺口，均属**事实**而非恢复故障：

- **2026-09-08 全天缺失** —— 已知缺陷「PC 关机/休眠 → 定时任务整天失败」的现场（文档第四部分🔴项）。
- **2026-09-10 无晚报** —— 系统当天 14:30 最后一次成功运行后即被清空，当晚晚报从未生成；前端正确显示「晚报待更新」占位。

### 本地预览验证方式

没有只看文件是否存在，而是**用本机 Edge 无头模式 + DevTools 协议真实渲染后截图**验证（Node 22 自带 `fetch` + 全局 `WebSocket`，零第三方依赖）。
验证结论：

- 页面标题 `A股速览 · 每日简报`；5 个 Tab = 早报 / 晚报 / 短线 / 量价 / 日历
- 默认落在「最新一期 = 2026年9月10日」
- 「量价」Tab 4 期可切换、「日历」Tab 3 篇、「短线」Tab 跟随早报/晚报
- 日期回看：最新 → `历史 · 2026-09-09` → `历史 · 2026-09-07`，内容随之切换，可正常返回最新

---

## 二、6 个定时任务（全部 ACTIVE）

| # | 名称 | 调度 | 下次触发 | 任务 ID |
|---|---|---|---|---|
| 1 | A股早报-开盘必读资讯 | 每天 08:30 | 09-13 周日 08:30 | `0edec1bb-7218-4991-a906-b99d789a9843` |
| 2 | A股量价选股 | 周一~周五 15:10 | 09-14 周一 15:10 | `5cff8748-1e37-48d9-ad29-282b141bfa4f` |
| 3 | A股晚报-AI读图复盘+投资日历专版 | 每天 21:00 | 09-12 今天 21:00 | `a7e3083b-c6da-4fe3-b6c4-1a69de6b7fb7` |
| 4 | A股次日验证 | 周一~周五 21:30 | 09-14 周一 21:30 | `7de2afd7-883b-40ac-af53-413f5b4ecf2d` |
| 5 | A股早报补跑看门狗 | 每天 10:00 | 09-13 周日 10:00 | `3e2314c2-3644-43ed-a84c-e3e9a21ba177` |
| 6 | A股晚报补跑看门狗 | 每天 22:30 | 09-12 今天 22:30 | `f72377e1-4d16-4db4-b6af-a31fd144df28` |

**相对原系统的两处变更（均按本次指令）：**

1. 量价选股 **14:30 → 15:10** —— 修正「盘中跑，分时快照使入选数被显著压低」的已知缺陷（实测 22→2 vs 收盘后 51→18）。
2. 晚报补跑看门狗 **PAUSED → ACTIVE** —— 原系统未启用。

**其他：** 工作目录统一为项目根；prompt 取自 `A股简报-定时任务提示词.md`，**仅删掉已被下线的通知步骤并顺延编号**（原文通知步骤位置：任务1第8步、任务2第6步、任务3第12步），其余一字未改，也未做任何"自作聪明"的逻辑优化。

---

## 三、需要你提供的东西

### 当前状态：**0 项**。

在「下线通知 + 先本地部署」的组合下，原清单上的凭据全部归零：

| 凭据 | 现在是否需要 | 原因 |
|---|---|---|
| GitHub PAT | ❌ 不需要 | 只用于推送到公网仓库；当前不推。post-commit 检测到无 `.gh-token` 会打印提示并 `exit 0`，**不会报错、不阻塞本地提交**（已实测） |
| Server酱 SendKey | ❌ 不需要 | 推送通知功能已整体下线 |
| LLM API Key | ❌ 不需要，且从来不落盘 | 抓取与提炼由**定时任务运行时的大模型**现场完成，系统本身不调用外部大模型 |
| `domainExpiry` | ⏳ 可选 | 仅在启用域名到期提醒时才需要填 |

> **补充说明（文档与实现不一致，已记录）**：重建文档写 Server酱 key 放 `.sct-key`，但 `tools/notify.js` 实际**只读环境变量 `SCT_KEY`**，项目里从来不存在 `.sct-key` 文件。日后若要恢复推送，需要新建或改造读取逻辑。

### 若日后要打开「云端发布」，你只需提供一样东西

**GitHub PAT**，放到项目根 `.gh-token`：**40 位 hex、纯单行、无空格无换行**（脚本直接 `cat` 后当 `Authorization` header 用）。需要 `contents: write` 权限。
仓库名不是从这份文件读的 —— 它硬编码在 `tools/gh_push_api.js` 的 `DEFAULT_REPO = 'zoushiyun1999/astock-dashboard'`，换仓库要改这里。

---

## 四、还没解决 / 需要你决定

### 1. GitHub 侧是否还活着（**需要你确认**）

`git remote -v` 为空 —— 推送不靠 git remote，靠 API。但我无法从本机判断：

- 仓库 `zoushiyun1999/astock-dashboard` 是否还存在？（Public 才能用 Pages）
- 域名 `asx.79zl.cn` 是否还在解析、是否已到期？`config/site.json` 的 `domainExpiry` 是空的。

**这决定本地跑完后线上是否会更新。** 现状是：本地正常，线上不会变。

### 2. 晚报任务的提示词有个缺口（**建议你决定是否补**）

任务 3 的原文只说「判断是否为今天发布，不是则带 `?t=` 复抓一次再判定」，**但没写"判定后仍不是今天"该怎么办**（早报那版有明确的跳过分支："记录今日未发布，跳过，直接结束"）。
周末与节假日存在生成脏数据的风险。今晚 21:00 该任务就会跑一次（今天 9/12 是周六）。

- **A（推荐）**：给任务 3 的 prompt 补一句显式兜底——「若判定后仍非今天发布，则记录『今日未发布，跳过』，不写数据、不发布、直接结束」，与早报保持一致。
- **B**：保持原样，依赖模型自行判断（原系统 9/5 周六确实自行做了幂等跳过）。
- **C**：把任务 3 与任务 6 的 rrule 改成只跑工作日。

### 3. 原系统 11 个已知缺陷，本次**未修**（保持原样恢复）

按「恢复」语义我没有顺手改代码，以下仍然存在，供你决定是否要治：

| 级别 | 问题 |
|---|---|
| 🔴 | PC 关机/休眠 → 定时任务整天失败（9/8 已发生一次） |
| 🟠 | `verify.gain` 语义误导：记的是「验证日收盘涨跌幅」，不是实盘收益（推荐股平均高开 3.78%，按开盘买入实际是亏的） |
| 🟠 | 行情范围只覆盖沪深主板，创业板/科创板/北交所的推荐股永远验证不到 |
| 🟠 | 早报 08:30 存在「假未发布」误判（已有看门狗兜底） |
| 🟡 | 新浪日K 数据源不稳定 |
| 🟡 | `config/site.json` 的 `domainExpiry` 为空 |
| 🟡 | 根目录 `一键推送GitHub.bat` 残留（`tmp_*` 本次已不带入） |

### 4. 文档未同步通知下线这一变更

`AGENTS.md`、`README.md`、`docs/云端部署方案.md`、`docs/系统功能与运行逻辑总览.md` 里仍写着「微信通知」链路。**代码已下线，文档尚未同步** —— 需要的话我可以一并改。

### 5. 本次新增/变更的文件清单（均未提交）

```
M  dashboard/index.html        （版本号 v=202609121942）
M  dashboard/version.json      （export_json 重新导出）
M  tools/notify.js             （顶部加禁用开关）
M  tools/notify_digest.js      （顶部加禁用开关）
D  tmp_*（5 项）                （按指令排除，未带入）
M  MEMORY.md                   （恢复记录 + 任务表更新 + 待办更新）
A  .workbuddy/memory/2026-09-12.md
A  docs/恢复完成报告-2026-09-12.md  （本文件）
```

---

## 五、硬性约束遵守情况

- ✅ 未改回 `workbuddy_sites_deploy` / cloudstudio 沙箱部署（旧记忆里残留该链路，未照抄）
- ✅ `dashboard/` 下未放任何临时文件
- ✅ `reports` 保持 newest-at-bottom；`bump_version.sh` 的 sort 步骤验证为 no-op（顺序本就正确）
- ✅ 未凭模型记忆填写个股代码（本次未生成任何新数据）
- ✅ 改完 `data.js` 相关已跑 `export_json`
- ✅ 未硬编码任何新链接
- ✅ 隔离区原件未动（35M / 102 提交 / `.gh-token` / `tmp_*` 全部原样）

---

_信息仅供参考，投资有风险。_
