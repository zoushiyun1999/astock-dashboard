# WorkBuddy 项目内部数据（任务状态/记忆），不进版本库
.workbuddy/

# GitHub CLI 配置（含登录凭据，绝不提交）
.gh-config/

# 访问令牌（绝不提交）
.gh-token
.env
*.token

# 任务临时文件（tmp_ 前缀，仅本地使用，绝不提交）
tmp_*

# Python 编译产物（tools/optimize_calendar.py 会生成，绝不提交）
__pycache__/
*.py[cod]

# 本地历史归档（旧项目源码 / 诊断报告 / 工作日志 / 验证截图）
# 含主机名、会话 ID 等本机信息，只留本地，不进版本库与公开仓库。
# 注意：gh_push_api.js 的 SKIP_DIR 也硬拦了「归档」，两道闸门缺一不可 ——
#       .gitignore 只管 git 提交，API 推送脚本不读它。
docs/归档/

# 数据快照目录（发布成功后自动快照 HEAD 版 data.js，保留 7 份）
# 体积大且是本地回滚保险，只留本地，不进版本库与公开仓库。
# 注意：gh_push_api.js 的 SKIP_DIR 也硬拦了「backups」，两道闸门缺一不可（规则 25）。
tools/backups/

